version: 2

sources:
  - name: better_help
    description: BigQuery data warehouse environment created in order to store better_help_full table below
    database: mind-diagnostics-414622  
    schema: md  
    tables:
      - name: better_help_full
        description: Sourced from HasOffers (aka Tune, https://www.tune.com/) and subsequently loaded into BigQuery data warehouse referenced above. When a user clicks out from our site over to BetterHelp and subsequently signs up we pass a number of data to them in addition to their own which we have access to.
        columns:
          - name: Stat_datetime
            description: '{{ doc("better_help") }}'
          - name: Goal_name
            description: '{{ doc("better_help") }}'
          - name: Country_name
            description: '{{ doc("better_help") }}'
          - name: Stat_ad_id
            description: '{{ doc("better_help") }}'
          - name: Stat_affiliate_info1
            description: '{{ doc("better_help") }}'
          - name: Stat_affiliate_info2
            description: '{{ doc("better_help") }}'
          - name: Stat_affiliate_info3
            description: '{{ doc("better_help") }}'
          - name: Stat_affiliate_info4
            description: '{{ doc("better_help") }}'
          - name: Stat_affiliate_info5
            description: '{{ doc("better_help") }}'
          - name: ConversionsMobile_affiliate_click_id
            description: '{{ doc("better_help") }}'
            